<%- include("partials/header") %>

<h1>HOME</h1>

<p><%= home %></p>

<% posts.forEach(function(post) { %>
    <span class="post-title"><h1> <%= post.title %> </h1> <button class="post-delete" type="submit"><a href="/<%= post._id %>">🗑️</a></button></span>
    <p> <%= post.content.substring(0, 157) + " ..." %> <a href="/posts/<%= post._id %>">Read More</a> </p>
<% }) %>

<%- include("partials/footer") %>